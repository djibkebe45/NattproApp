name: natt_pro_app
description: NattPro - Tontine Sénégalaise

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_contacts: ^1.1.9+2
  permission_handler: ^11.3.1

flutter:
  uses-material-design: true
